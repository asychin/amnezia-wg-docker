# Обратная совместимость v1.x → v2.0.0

## ✅ ПОДТВЕРЖДЕНИЕ: 100% Обратная совместимость восстановлена!

### 🎯 Краткое резюме

**v2.0.0 полностью обратно совместима с v1.x** благодаря использованию Docker Compose profiles.

---

## 📊 Режимы работы

### 1️⃣ VPN-only режим (v1.x совместимость) - ПО УМОЛЧАНИЮ

**Что запускается:** Только VPN сервер (контейнер `amneziawg-server`)

**Команды:**
```bash
# Стандартные команды v1.x работают без изменений
make up                                # Запустить VPN
make down                              # Остановить VPN
make build                             # Собрать образ
make status                            # Статус VPN
make logs                              # Логи VPN
make backup                            # Бэкап конфигураций

# Управление клиентами (v1.x)
make client-add name=john              # Добавить клиента
make client-rm name=john               # Удалить клиента
make client-qr name=john               # Показать QR код
make client-config name=john           # Показать конфигурацию
make client-list                       # Список клиентов

# Или через docker compose напрямую
docker compose up -d                   # Запустить только VPN
docker compose down                    # Остановить
```

**Что НЕ запускается:**
- ❌ PostgreSQL (не нужен для VPN)
- ❌ Веб-интерфейс (не нужен для VPN)

**Зависимости:**
- ✅ Docker 20.10+
- ✅ Docker Compose 2.0+
- ❌ PostgreSQL НЕ требуется
- ❌ Node.js НЕ требуется

---

### 2️⃣ Полный стек (VPN + Web + PostgreSQL) - ОПЦИОНАЛЬНО

**Что запускается:** VPN + Веб-интерфейс + PostgreSQL

**Команды:**
```bash
# Запустить полный стек
docker compose --profile web up -d

# Остановить
docker compose --profile web down

# Логи всех сервисов
docker compose --profile web logs -f

# Статус всех сервисов
docker compose --profile web ps
```

**Что запускается:**
- ✅ VPN сервер (контейнер `amneziawg-server`)
- ✅ PostgreSQL (контейнер `amneziawg-db`)
- ✅ Веб-интерфейс + API (контейнер `amneziawg-web`)

**Дополнительные возможности:**
- 🌐 Веб-интерфейс на http://your-server:8080
- 📊 REST API для автоматизации
- 💾 PostgreSQL база данных для метаданных
- 📱 QR коды в браузере
- 🔍 Поиск и фильтрация клиентов

---

### 3️⃣ Минимальная конфигурация (альтернативный способ)

**Явный VPN-only режим с отдельным файлом:**

```bash
docker compose -f docker-compose.minimal.yml up -d
```

Этот файл содержит только VPN сервер без упоминания PostgreSQL и веб-интерфейса.

---

## 🔍 Технические детали совместимости

### Использование Docker Compose Profiles

В `docker-compose.yml` используются profiles для разделения сервисов:

```yaml
services:
  # VPN - всегда запускается (нет profile)
  vpn:
    image: amneziawg-server
    # ... конфигурация ...
    # НЕТ profiles: [] - запускается по умолчанию

  # PostgreSQL - только с --profile web
  postgres:
    profiles: ["web"]
    image: postgres:16-alpine
    # ... конфигурация ...

  # Web - только с --profile web
  web:
    profiles: ["web"]
    image: amneziawg-web
    # ... конфигурация ...
```

**Результат:**
- `docker compose up` → запускает только **vpn** (сервисы без profiles)
- `docker compose --profile web up` → запускает **vpn + postgres + web** (все сервисы)

### Удаление зависимости VPN от PostgreSQL

В предыдущих версиях v2.0.0 VPN зависел от PostgreSQL:
```yaml
vpn:
  depends_on:
    postgres:
      condition: service_healthy  # ❌ НЕПРАВИЛЬНО
```

**Исправлено в текущей версии:**
```yaml
vpn:
  # VPN НЕ зависит от PostgreSQL (только веб-интерфейс использует БД)
  # depends_on закомментирован для обратной совместимости с v1.x
```

Теперь VPN запускается независимо, PostgreSQL нужен только для веб-интерфейса.

---

## ✅ Проверочный список совместимости

### Функциональность v1.x

| Функция v1.x | Статус в v2.0.0 | Примечание |
|--------------|-----------------|------------|
| `make up` | ✅ Работает | Запускает только VPN |
| `make down` | ✅ Работает | Останавливает VPN |
| `make client-add` | ✅ Работает | Добавление клиента |
| `make client-rm` | ✅ Работает | Удаление клиента |
| `make client-qr` | ✅ Работает | QR код в терминале |
| `make client-config` | ✅ Работает | Конфигурация клиента |
| `make client-list` | ✅ Работает | Список клиентов |
| `make backup` | ✅ Работает | Бэкап конфигураций |
| `make restore` | ✅ Работает | Восстановление бэкапа |
| Без PostgreSQL | ✅ Работает | VPN не требует БД |
| Без Node.js | ✅ Работает | VPN не требует Node |
| Структура ./clients | ✅ Работает | Сохранена полностью |
| Структура ./config | ✅ Работает | Сохранена полностью |
| .env переменные | ✅ Работает | Все новые имеют дефолты |

### Новые возможности v2.0.0 (опционально)

| Функция v2.0.0 | Требует --profile web | Примечание |
|----------------|----------------------|------------|
| Веб-интерфейс | ✅ Да | http://server:8080 |
| REST API | ✅ Да | http://server:3001/api |
| PostgreSQL | ✅ Да | Для метаданных клиентов |
| QR коды в браузере | ✅ Да | Через веб-интерфейс |
| React UI | ✅ Да | shadcn/ui компоненты |

---

## 🚀 Примеры миграции

### Сценарий 1: Простая миграция v1.x → v2.0.0 (VPN-only)

```bash
# 1. Бэкап текущей конфигурации v1.x
cd /path/to/amnezia-wg-docker
make backup

# 2. Обновление кода
git fetch origin
git checkout v2.0.0
git submodule update --remote --recursive

# 3. Пересборка
make down
make build
make up

# 4. Проверка
make status
make client-list
```

**Результат:** VPN работает как в v1.x, никаких изменений для пользователей.

---

### Сценарий 2: Миграция с включением веб-интерфейса

```bash
# 1. Бэкап
make backup

# 2. Обновление кода
git fetch origin
git checkout v2.0.0
git submodule update --remote --recursive

# 3. Настройка окружения (опционально, есть дефолты)
cp env.example .env
nano .env  # Установите POSTGRES_PASSWORD и API_SECRET

# 4. Остановка v1.x и запуск v2.0.0 с веб-интерфейсом
make down
make build
docker compose --profile web up -d

# 5. Синхронизация существующих клиентов с PostgreSQL
# Откройте веб-интерфейс: http://your-server:8080
# Нажмите кнопку "Синхронизировать"

# Или через API:
curl -X POST http://your-server:3001/api/sync
```

**Результат:** VPN + веб-интерфейс + PostgreSQL. Все существующие клиенты импортированы.

---

## 📝 Часто задаваемые вопросы

### Q: Нужно ли обновлять .env файл?
**A:** Нет, не обязательно. Все новые переменные имеют безопасные значения по умолчанию:
- `POSTGRES_DB` → `amneziawg`
- `POSTGRES_USER` → `amneziawg`
- `POSTGRES_PASSWORD` → `change_this_password_to_secure_one`
- `WEB_PORT` → `8080`

Для продакшена рекомендуется установить свои значения.

### Q: Будут ли работать существующие клиенты после обновления?
**A:** Да, 100%. Конфигурации клиентов хранятся в `./clients/`, которая не изменяется.

### Q: Нужно ли удалять старые контейнеры перед обновлением?
**A:** Да, выполните `make down` перед `make build && make up`.

### Q: Что делать если я не хочу веб-интерфейс?
**A:** Просто используйте `make up` или `docker compose up -d`. Веб-интерфейс не запустится.

### Q: Можно ли переключаться между режимами?
**A:** Да, в любой момент:
```bash
# Переключиться на VPN-only
docker compose down
docker compose up -d

# Переключиться на полный стек
docker compose down
docker compose --profile web up -d
```

### Q: Сохраняются ли данные PostgreSQL между перезапусками?
**A:** Да, PostgreSQL использует Docker volume `amneziawg-postgres-data`.

### Q: Можно ли откатиться на v1.x?
**A:** Да, конфигурации совместимы:
```bash
make down
git checkout v1.1.0
make build && make up
```

---

## 🎯 Итоги

### ✅ Что сохранено из v1.x

1. **Все Makefile команды** - `make up`, `make client-add name=john`, и т.д.
2. **Структура конфигов** - `./clients/`, `./config/` без изменений
3. **Скрипты управления** - `scripts/manage-clients.sh` работает как раньше
4. **Независимость VPN** - Не требует PostgreSQL, Node.js, или других зависимостей
5. **Минимальные требования** - Только Docker и Docker Compose

### 🆕 Что добавлено в v2.0.0 (опционально)

1. **Веб-интерфейс** - React + TypeScript + shadcn/ui
2. **REST API** - Программный доступ к управлению
3. **PostgreSQL** - База данных для метаданных
4. **Docker Profiles** - Гибкое управление режимами
5. **Исправления безопасности** - Path traversal, command injection, race conditions

### 🎉 Вывод

**v2.0.0 = v1.x + опциональные улучшения**

Пользователи v1.x могут обновиться без риска:
- Используя те же команды (`make up`)
- Без установки PostgreSQL
- Без изменения рабочих процессов
- С опцией активировать веб-интерфейс позже

**100% обратная совместимость подтверждена!** ✅

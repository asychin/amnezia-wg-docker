# Руководство по миграции AmneziaWG v1.1.0 → v2.0.0

## ⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ

**ДЛЯ ПРОДАКШЕНА ОБЯЗАТЕЛЬНО:**

1. **Установите API_SECRET** для защиты API endpoints:
   ```bash
   export API_SECRET="your-very-long-random-secret-here-min-32-chars"
   ```

2. **Используйте HTTPS** (не HTTP) для всех подключений к API

3. **Настройте firewall** - ограничьте доступ к портам 3001, 5000

4. **Регулярно обновляйте** зависимости: npm audit fix

Без этих мер ваши VPN конфигурации могут быть скомпрометированы!

---

## Обзор изменений

Версия 2.0.0 включает критические исправления безопасности и новые функции при сохранении 100% обратной совместимости с версией 1.1.0.

### ⚠️ Критические исправления безопасности

1. **Path Traversal / Command Injection** - исправлены уязвимости в `scripts/manage-clients.sh`
2. **Утечка приватных ключей** - улучшены права доступа к файлам
3. **Race Conditions** - добавлены блокировки конфигурационных файлов
4. **Баг с IP адресами** - исправлена проблема определения IPv4 на dual-stack системах

### 🆕 Новые возможности

1. **Веб-интерфейс управления** - React + shadcn/ui интерфейс для управления клиентами
2. **PostgreSQL база данных** - централизованное хранение метаданных пользователей
3. **REST API** - программный доступ к управлению клиентами
4. **Улучшенная диагностика** - обновленный healthcheck с поддержкой `ss`

---

## Миграция для существующих развертываний

### ✅ Безопасная миграция без даунтайма

Все изменения **полностью обратно совместимы**. Ваши существующие клиенты и конфигурации продолжат работать без изменений.

### Шаг 1: Создание резервной копии

**Перед обновлением обязательно создайте бекап:**

```bash
# На работающем сервере
make backup

# Проверьте что бекап создан
ls -lh amneziawg-backup-*.tar.gz
```

Сохраните файл бекапа в безопасном месте (не на том же сервере).

### Шаг 2: Обновление кода

```bash
# Перейдите в директорию проекта
cd amnezia-wg-docker

# Сохраните текущую конфигурацию
cp .env .env.backup

# Получите обновления
git fetch origin
git checkout v2.0.0

# Обновите субмодули
git submodule update --remote --recursive
```

### Шаг 3: Проверка конфигурации

Проверьте ваш `.env` файл. В версии 2.0.0 добавлены новые опциональные параметры:

```bash
# Новые опциональные переменные (можно не добавлять)

# Для веб-интерфейса (если хотите использовать)
# DATABASE_URL=postgresql://user:password@localhost:5432/amneziawg
# API_PORT=3001
# FRONTEND_PORT=5000
```

**Важно:** Ваши существующие настройки работают без изменений!

### Шаг 4: Пересборка образа

```bash
# Остановите старый контейнер
make down

# Пересоберите образ (с учетом исправлений)
make build

# Запустите обновленный сервер (ТОЛЬКО VPN, как в v1.x)
make up

# Или запустите с веб-интерфейсом (опционально)
# docker compose --profile web up -d
```

**Важно:** По умолчанию `make up` и `docker compose up -d` запускают **только VPN сервер** (полная совместимость с v1.x). Веб-интерфейс и PostgreSQL запускаются только при явном указании профиля `web`.

### Шаг 5: Проверка работоспособности

```bash
# Проверьте статус сервера
make status

# Проверьте логи
make logs

# Проверьте список клиентов
make client-list
```

**Ожидаемый результат:** Все ваши существующие клиенты должны быть видны и продолжать работать.

### ⚡ Режимы работы v2.0.0

**1. VPN-only режим (v1.x совместимость) - по умолчанию:**
```bash
make up                          # Или docker compose up -d
```
Запускает только VPN сервер без PostgreSQL и веб-интерфейса.

**2. Полный стек (VPN + Web + PostgreSQL):**
```bash
docker compose --profile web up -d
```
Запускает VPN + веб-интерфейс + PostgreSQL.

**3. Минимальная конфигурация (альтернатива):**
```bash
docker compose -f docker-compose.minimal.yml up -d
```
Явный VPN-only режим с отдельным файлом.

### Шаг 6 (опционально): Настройка веб-интерфейса

**v2.0.0 полностью обратно совместима с v1.x!** По умолчанию работает только VPN сервер без дополнительных зависимостей.

Если хотите использовать новый веб-интерфейс (React + PostgreSQL):

```bash
# 1. Установите PostgreSQL (если еще не установлен)
# На Ubuntu/Debian:
sudo apt-get install postgresql postgresql-contrib

# 2. Создайте базу данных
sudo -u postgres createdb amneziawg

# 3. Создайте пользователя
sudo -u postgres psql
# В psql:
CREATE USER amneziawg WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE amneziawg TO amneziawg;
\q

# 4. Добавьте DATABASE_URL в .env
echo "DATABASE_URL=postgresql://amneziawg:your_secure_password@localhost:5432/amneziawg" >> .env

# 5. Запустите миграции базы данных
npm run db:push

# 6. Запустите веб-интерфейс
npm run dev
```

Веб-интерфейс будет доступен на `http://your-server:5000`

---

## Что изменилось под капотом

### 1. Исправления безопасности

#### scripts/manage-clients.sh

**Было:**
```bash
# Принимал любые имена клиентов без валидации
CLIENT_NAME=$1
```

**Стало:**
```bash
# Валидация имен - только безопасные символы
if [[ ! "$CLIENT_NAME" =~ ^[A-Za-z0-9_-]+$ ]]; then
    error "Недопустимое имя клиента. Используйте только буквы, цифры, _ и -"
    exit 1
fi
```

**Защита от:**
- Path traversal (`../../etc/passwd`)
- Command injection (`; rm -rf /`)
- Специальные символы в именах файлов

#### scripts/manage-clients.sh - Блокировки

**Добавлено:**
```bash
# Блокировка конфигурационного файла
exec 200>"${CONFIG_FILE}.lock"
flock -x 200
# ... операции с конфигом ...
flock -u 200
```

**Защита от:** Race conditions при одновременном добавлении/удалении клиентов

#### Dockerfile - Права доступа

**Было:**
```dockerfile
chmod 750 /app/clients
```

**Стало:**
```dockerfile
chmod 700 /app/clients
ENV UMASK=077
```

**Защита от:** Чтения приватных ключей другими пользователями

### 2. Исправление бага с IP адресами

#### scripts/entrypoint.sh - get_public_ip()

**Проблема:** На dual-stack серверах (IPv4+IPv6) curl без флага `-4` получал IPv6 адреса, которые отклонялись regex проверкой IPv4.

**Решение:**

```bash
# Приоритетный метод - через локальный роутинг
PUBLIC_IP=$(ip -4 route get 1.1.1.1 2>/dev/null | grep -oP 'src \K\S+' || echo "")

if [ -n "$PUBLIC_IP" ]; then
    log "✅ IP определен через ip route: $PUBLIC_IP"
else
    # Fallback на curl с принудительным IPv4
    response=$(curl -4 -s --connect-timeout 10 "$service" 2>/dev/null)
fi
```

**Также исправлено:** Защита от read-only `/etc/resolv.conf` на systemd-resolved системах:

```bash
# Проверка возможности записи
if [ -w "/etc/resolv.conf" ]; then
    echo "nameserver 8.8.8.8" > /etc/resolv.conf
else
    warn "Не могу изменить /etc/resolv.conf (read-only)"
fi
```

### 3. Улучшения healthcheck.sh

**Было:**
```bash
netstat -tulpn | grep ":${AWG_PORT}"
```

**Стало:**
```bash
# Приоритет: ss → netstat → lsof
if command -v ss >/dev/null 2>&1; then
    ss -ulpn | grep -q ":${AWG_PORT}"
elif command -v netstat >/dev/null 2>&1; then
    netstat -tulpn | grep -q ":${AWG_PORT}"
fi
```

---

## Обратная совместимость

### ✅ Гарантированная совместимость

1. **Конфигурационные файлы** - формат не изменился
2. **Клиентские конфигурации** - продолжают работать без изменений
3. **Makefile команды** - все команды работают как раньше
4. **Переменные окружения** - все старые переменные поддерживаются
5. **Docker образ** - использует те же порты и volumes

### 📦 Volumes и данные

Все ваши данные сохраняются:
- `/app/config` - конфигурация сервера
- `/app/clients` - конфигурации клиентов
- Ключи и сертификаты

**Никакие данные не удаляются и не модифицируются при обновлении!**

### 🔄 Откат на предыдущую версию

Если что-то пошло не так, откат очень простой:

```bash
# 1. Остановите контейнер
make down

# 2. Восстановите из бекапа
tar -xzf amneziawg-backup-YYYYMMDD-HHMMSS.tar.gz

# 3. Вернитесь на предыдущую версию
git checkout v1.1.0
git submodule update --recursive

# 4. Пересоберите и запустите
make build
make up
```

---

## Новые возможности (опционально)

### Веб-интерфейс

Если хотите использовать новый веб-интерфейс:

**Возможности:**
- Управление клиентами через браузер
- Просмотр QR кодов
- Экспорт конфигураций
- История подключений
- Статистика использования

**Требования:**
- PostgreSQL 12+
- Node.js 18+ (для разработки)
- Дополнительные порты: 5000 (frontend), 3001 (API)

**Настройка описана в Шаге 6 выше.**

### REST API

Новый API доступен для автоматизации:

```bash
# Получить список клиентов
curl http://localhost:3001/api/clients

# Добавить клиента
curl -X POST http://localhost:3001/api/clients \
  -H "Content-Type: application/json" \
  -d '{"name": "newclient", "ipAddress": "10.13.13.15"}'

# Удалить клиента
curl -X DELETE http://localhost:3001/api/clients/newclient

# Получить QR код
curl http://localhost:3001/api/clients/newclient/qr

# Получить конфигурацию
curl http://localhost:3001/api/clients/newclient/config
```

---

## Часто задаваемые вопросы (FAQ)

### Q: Нужно ли мне обновляться?

**A:** Да, настоятельно рекомендуется. Версия 2.0.0 исправляет критические уязвимости безопасности:
- Path traversal (доступ к произвольным файлам)
- Command injection (выполнение произвольных команд)
- Утечка приватных ключей

### Q: Будут ли работать мои существующие клиенты после обновления?

**A:** Да, абсолютно все существующие клиенты продолжат работать без каких-либо изменений. Обратная совместимость 100%.

### Q: Нужно ли мне использовать веб-интерфейс?

**A:** Нет, это опционально. Все старые команды `make client-add`, `make client-rm` и т.д. продолжают работать как раньше.

### Q: Что делать если обновление сломало мой сервер?

**A:** Используйте бекап для отката на предыдущую версию (см. раздел "Откат на предыдущую версию").

### Q: Нужно ли переконфигурировать клиентов?

**A:** Нет, клиентские конфигурации не меняются. Клиентам ничего делать не нужно.

### Q: Изменились ли требования к системе?

**A:** Нет, минимальные требования остались те же:
- Docker 20.10+
- 512 MB RAM (1 GB рекомендуется)
- 1 GB свободного места

Веб-интерфейс (опционально) добавляет требования:
- PostgreSQL 12+ (если хотите использовать веб-интерфейс)
- +256 MB RAM для веб-интерфейса

### Q: Как проверить что обновление прошло успешно?

**A:** Выполните:

```bash
# Проверьте версию
cat VERSION  # Должно быть 2.0.0

# Проверьте статус
make status

# Проверьте клиентов
make client-list
```

Если все команды работают и клиенты подключаются - обновление прошло успешно.

---

## Помощь и поддержка

### Сообщить о проблеме

Если вы столкнулись с проблемами при миграции:

1. **GitHub Issues:** https://github.com/yourusername/amnezia-wg-docker/issues
2. **Discussions:** https://github.com/yourusername/amnezia-wg-docker/discussions
3. **Telegram:** https://t.me/amneziawg

### Диагностика проблем

```bash
# Полная диагностика
make debug

# Проверка healthcheck
docker exec amneziawg-server /app/scripts/healthcheck.sh

# Проверка логов
make logs
```

---

## Контрольный список миграции

Используйте этот список для проверки:

- [ ] Создан резервный бекап (`make backup`)
- [ ] Скопирован `.env` файл (`.env.backup`)
- [ ] Обновлен код из git (`git checkout v2.0.0`)
- [ ] Обновлены субмодули (`git submodule update`)
- [ ] Остановлен старый контейнер (`make down`)
- [ ] Пересобран образ (`make build`)
- [ ] Запущен новый контейнер (`make up`)
- [ ] Проверен статус (`make status`)
- [ ] Проверен список клиентов (`make client-list`)
- [ ] Проверено подключение клиента
- [ ] (Опционально) Настроен веб-интерфейс
- [ ] (Опционально) Настроена база данных PostgreSQL

---

## Резюме

**Версия 2.0.0** - это важное обновление безопасности с новыми функциями при сохранении полной обратной совместимости.

**Ключевые моменты:**
- ✅ 100% обратная совместимость
- ✅ Критические исправления безопасности
- ✅ Безопасная миграция без даунтайма
- ✅ Легкий откат при проблемах
- ✅ Опциональный веб-интерфейс

**Рекомендация:** Обновитесь как можно скорее для получения исправлений безопасности.

---

**Версия документа:** 1.0  
**Дата:** 2024-11-23  
**Применимо к:** AmneziaWG Docker Server v1.1.0 → v2.0.0
